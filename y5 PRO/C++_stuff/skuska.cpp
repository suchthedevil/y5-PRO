#include <iostream> /* first two lines are always these two*/
using namespace std; /*enables to use names form the standar library (somathing like import in python)*/

int main() {
    cout << "In C++, cout objects do not add a new line at the end";
    cout << "For real no lies\n"; /*cout is pronounced [see-out]*/
    cout << "Now it's a new line";
    return 0;
}
